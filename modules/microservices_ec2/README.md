# microservices_ec2 module

This is a temporary module to house all the logic for an EC2 instance that runs a lot of the small background jobs for App. The plan is to eventually move everything to ECS.
